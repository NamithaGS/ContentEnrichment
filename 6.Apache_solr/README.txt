load_solr.py

This program can be run to load all the metadata into Solr which should be running on localhost (http://localhost:8983/).

Run this on the command prompt as follows:

load_solr.py <top-level folder where solr is installed directly above the "dist" folder> <Path of the output file>
 